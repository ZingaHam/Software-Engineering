version: 1
frontend:
  build:
    commands:
    artifacts:
      baseDirectory: /
      files:
        - '**/*'
    cache:
      paths: []

backend:
  phases:
    preBuild:
      commands: npm i -g @aws-amplify/cli@10.5.0
    build:
      commands: amplify push --simple
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
